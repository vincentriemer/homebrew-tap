# homebrew-tap

Homebrew tap for `ios-simulator`.

Install with:

```bash
brew tap vincentriemer/tap
brew install ios-simulator
```

The formula in `Formula/ios-simulator.rb` is updated by the release workflow in the main `ios-simulator-cli` repository.
